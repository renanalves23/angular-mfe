{
  "name": "backend",
  "version": "1.0.0",
  "description": "Simulated REST API using json-server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000 --host 0.0.0.0 --cors",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  },
  "author": "",
  "license": "ISC"
}
